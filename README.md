# Docker Node Web Stack
Basic node web-app stack that includes the following:
 - NGINX (static HTML & proxy to Node API)
 - Node (api server)
 - phpMyAdmin
 - MariaDB

## Running
Run the following from the project's root directory:
```
docker-compose up -d
```